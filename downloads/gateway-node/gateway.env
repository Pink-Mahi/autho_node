# Autho Gateway Configuration
#
# This file is OPTIONAL. If present, the gateway will load it automatically on startup.
# Values here are used only when the same variable is not already set in your system environment.
#
# Tip: to make your gateway PUBLIC, use the "Autho Gateway (Public)" desktop shortcut.
#

# -------------------------
# Ports (optional)
# -------------------------
# GATEWAY_PORT=3001
# GATEWAY_WS_PORT=4001

# -------------------------
# Public URL (optional)
# -------------------------
# If you have a stable public URL (your own domain, or a Cloudflare custom hostname), put it here.
# This is the URL the gateway will advertise to the network.
# GATEWAY_PUBLIC_URL=https://your-gateway.yourdomain.com

# -------------------------
# Cloudflare Tunnel mode (optional)
# -------------------------
# Default is quick/random tunnel URLs (trycloudflare.com).
# To use a stable Cloudflare tunnel, create a named tunnel in Cloudflare Zero Trust,
# configure its Public Hostname + service to point to http://localhost:3001,
# then paste its token below.
#
# Modes:
# - quick (default): Cloudflare quick tunnel (random URL)
# - cloudflare_named: Cloudflare named tunnel (stable if you use a custom hostname)
#
# GATEWAY_TUNNEL_MODE=quick
# CLOUDFLARED_TOKEN=

# -------------------------
# Scaling & Performance (optional)
# -------------------------
# These settings control how much memory and storage your gateway uses.
# Defaults are tuned for a single-user gateway on a regular PC.
# If you're running on a powerful server, increase these limits.

# Message retention in days (default: 5 for gateways, 10 for operators)
# AUTHO_MSG_RETENTION_DAYS=5

# Max messages to keep in memory before oldest are evicted (default: 5000 for gateways)
# AUTHO_MAX_MESSAGES=5000

# Max heap memory in MB before emergency message eviction (default: 512 for gateways)
# AUTHO_MAX_HEAP_MB=512

# LRU event cache size — how many ledger events to cache in RAM (default: 2000 for gateways)
# AUTHO_EVENT_CACHE_SIZE=2000

# Per-conversation media limits
# AUTHO_MAX_IMAGES=10
# AUTHO_MAX_AUDIO=5
# AUTHO_MAX_VIDEO=3

# Bridge operators for censorship bypass (comma-separated URLs)
# AUTHO_BRIDGE_OPERATOR_URLS=https://bridge1.example.com,https://bridge2.example.com
